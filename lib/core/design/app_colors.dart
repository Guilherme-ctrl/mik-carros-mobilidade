import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const brandPink      = Color(0xFFE91E8C);
  static const brandPinkDark  = Color(0xFFB5146D);
  static const brandPinkLight = Color(0xFFFF4DB2);
  static const brandPinkMuted = Color(0xFF3D0A25);

  // Surfaces
  static const surface0       = Color(0xFF0D0D18);
  static const surface1       = Color(0xFF16162A);
  static const surface2       = Color(0xFF21213A);
  static const surface3       = Color(0xFF2D2D4E);

  // On-surface
  static const onSurface         = Color(0xFFF2F2F2);
  static const onSurfaceMuted    = Color(0xFF8A8AB0);
  static const onSurfaceDisabled = Color(0xFF44445A);

  // Status — mission phases
  static const statusAvailable    = Color(0xFF22C55E);
  static const statusAvailableBg  = Color(0xFF0D2E1A);
  static const statusBusy         = Color(0xFFF59E0B);
  static const statusBusyBg       = Color(0xFF2E1F05);
  static const statusUnavailable  = Color(0xFFEF4444);
  static const statusUnavailableBg= Color(0xFF2E0808);
  static const statusPending      = Color(0xFF60A5FA);
  static const statusPendingBg    = Color(0xFF0A1829);
  static const statusDone         = Color(0xFFA78BFA);
  static const statusDoneBg       = Color(0xFF150D2E);

  // Semantic
  static const error   = Color(0xFFEF4444);
  static const errorBg = Color(0xFF2E0808);
  static const warning = Color(0xFFF59E0B);
  static const success = Color(0xFF22C55E);
  static const info    = Color(0xFF60A5FA);
}
